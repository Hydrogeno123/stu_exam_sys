<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.AnswerRecordMapper">

    <!--批量新增答题记录-->
    <insert id="batchInsert">
        insert into answer_record(exam_id, student_id, question_id, student_answer, is_correct, score, submit_time)
        values
        <foreach collection="list" item="record" separator=",">
            (#{record.examId}, #{record.studentId}, #{record.questionId}, #{record.studentAnswer}, #{record.isCorrect}, #{record.score}, #{record.submitTime})
        </foreach>
    </insert>

</mapper>
